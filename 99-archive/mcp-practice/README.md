# MCP practice

**Что это:** учебный мини-проект с REST API и веб-UI — обёртка вокруг практики MCP (см. подпапку `coin_flip_api/`).

**Главная документация:** [`coin_flip_api/README.md`](./coin_flip_api/README.md) — запуск через Docker Compose, эндпоинты, интерфейс.

**Быстрый старт:** из каталога `coin_flip_api/` выполнить `docker compose up -d --build` и открыть http://localhost:5000 (как в README подпроекта).
