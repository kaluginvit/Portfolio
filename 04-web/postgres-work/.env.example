# Telegram (обязательно)
TELEGRAM_BOT_TOKEN=123456789:AA-your-bot-token-from-BotFather

# PostgreSQL — для docker-compose ниже используйте те же DB_* что и POSTGRES_* в compose
DB_HOST=localhost
DB_PORT=5432
DB_NAME=telegram_forms
DB_USER=botuser
DB_PASSWORD=botpass

# Опционально: если подняли compose с другим пробросом порта
# DB_PORT=5433
