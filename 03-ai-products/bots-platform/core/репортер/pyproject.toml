[project]
name = "bot-reporter"
version = "0.1.0"
description = "Telegram reporter for Money Alert AI"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.1",
    "python-telegram-bot>=21.0",
    "httpx>=0.27.0",
]
